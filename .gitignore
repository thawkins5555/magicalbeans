__pycache__/
*.pyc
*.pyo
*.db
*.db-journal
*.db-wal
*.db-shm
.venv/
venv/
*.egg-info/
update_restart.log
netpath.bak-*/

# Claude Code agent worktrees (tool state, never source)
.claude/

# The demo SSH double keeps one host key per machine so a restart is not a MITM
demo/fake_ssh_host_key
