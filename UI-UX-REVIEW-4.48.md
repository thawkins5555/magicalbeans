# UI/UX Review — SappiWhere 4.48.0

**Date:** September 4, 2026
**Build reviewed:** 4.47.0, commit `fc6b3e1` (`netpath/web/static/*`, `netpath/web/server.py`, `netpath/web/wsock.py`, `netpath/configrx.py`, `netpath/theme.py`)
**Branch:** `claude/ui-ux-review-improvements-ylkdll`, at the tip that became 4.48.0
**Reviewers:** two specialist passes (graphic/UI design; UX and interaction design), plus supporting passes for accessibility, the five operator-requested changes, a disposition of the 114 findings in `UI-UX-REVIEW.md` (build 4.36.1), and the test/release conventions — followed, once the fix work substantially landed, by two further read-only passes across the whole branch: a security review, then a broader one. Both sets of findings are recorded here on the same terms as everything else.

## A note on how this document was assembled

The six reviewers' own reports did not survive the machine that produced them. What survived is `FINDINGS.md` — a recovery document listing every finding the approved remediation plan scheduled for work, organized into the same eight phases the review used — and the commit history of this branch, which is where the reasoning and the measured numbers live. This document is built from those two sources: FINDINGS.md for what was raised, `git log` and `git show` for what was actually done about it. Where a number appears below, it is a number a commit measured, not one reconstructed after the fact.

This is the second full pass at this document. The first was written after seventeen commits, with Phase 6 not yet started, Phase 4's module adoption sitting uncommitted, and roughly thirty of the items in "What remains open" still open that a further twenty-two commits — forty in total — went on to close. That first pass is not what follows: every phase heading, every narrative and every row of the open-items table below has been checked again against the branch as it now stands, commit by commit, with the code read rather than the commit message alone taken on trust — which is how the correction recorded under Phase 7 below (the Nodes-refresh figure a later commit claimed to have fixed, and had not) was found, on top of two the branch had already corrected of itself along the way (an interface-dialog measurement, and a NetPath rebuild that turned out never to have been happening), and how two findings this revision's own drafting first credited to the wrong commit were caught and fixed before they shipped (recorded under Phase 4 below, where they belong). A final read-only code review of the finished branch found five defects of its own — none in what the fix work was asked to do, all in how it was done — and called the branch unfit to merge as it stood; `c05ed12` fixes all five. A third pass after that, recorded under Phase 8, read the module files against each other rather than each against the brief, and closed five duplications the per-file division of work had created.

## What was reviewed, and how

The review was driven against a live seeded instance rather than against the source alone: 30 simulated SNMP devices on a `127.0.0.2–31` range with fleet control on a separate port, seven fake SSH switches (`demo/fake_ssh.py` — a Cisco IOS device, a Cisco with a pager that never turns off, a Cisco whose capture truncates, a Fortinet, a MikroTik, a menu-driven console, and a device stuck at an unprivileged prompt), three accounts spanning admin/operator/read-only permissions, three themes (dark, light, high contrast) and three viewports (1920×1080, 1366×768, 1280×720). ConfigRX's Phase 2 work added five more SSH personas during the fix, covering NX-OS, IOS-XR, a small-business switch, an ASA and a WLC — the fleet the review was run against is smaller than the one the product now ships tests against.

The review's own screenshot collector, `demo/ui_walk.mjs`, is discussed on its own terms in the closing section: its inventory had gaps that the fix work found and closed, which is itself evidence about what the original review could and could not have seen.

---

## Three defects the review did not find

These were not on the reviewers' list. Each was found while fixing something else, and each is the kind of thing a review driven from screenshots and static reading is unlikely to catch, because the failure only shows up under a specific runtime condition — a hung device, a platform's own connection-reset behaviour, or ten minutes of an idle tab.

**A device that hangs up mid-capture crashed the ConfigRX backup instead of reporting the case its own error text exists for.** `_pull_config`'s `finally: channel.close()` (`netpath/configrx.py:511-521`) could raise out of the function and discard a result that had already been computed. Closing a channel sends a message; when the device has already hung up — precisely the `"closed"` case `_capture_problem` (`netpath/configrx.py:540`) exists to describe — paramiko's `write_all` raises `EOFError` into a socket that is gone. So the one situation the code was careful to name ("The device closed the connection before the config finished") instead crashed the whole attempt with a bare `EOFError`. Fixed in `b245e526` by making the close best-effort, matching the pattern the SSH terminal's own teardown already used. Found while chasing an intermittent failure in the new six-platform Cisco test suite that turned out not to be a flaky test at all.

**A refused WebSocket lost the sentence explaining the refusal, on Windows.** `WebSocket.close()` (`netpath/web/wsock.py`) sends its close frame — carrying the code and the sentence explaining itself, "There are already 16 SSH sessions" among them — then shuts the socket down in both directions, deliberately, because a half-closed socket is what unblocks a `recv()` parked in another thread. Windows resets a connection that is closed while data the peer sent is still unread, and a reset discards whatever was last written in the other direction. So the close frame was thrown away by the reset that followed it: the peer read a bare `ConnectionResetError` where it should have read the refusal, and an operator turned away by the session cap saw only a disconnection. Linux sends a FIN in the same circumstance, which is why every previous run of the test that depends on this passed. Fixed in `17aee347` by draining the receive buffer (bounded to eight non-blocking reads) before the shutdown, which took the Windows test run to 53 of 53 for the first time.

**The Debug tab's DOM churn was `App.wireRowKeyboard` restamping every row on every poll, not the table rebuild it was assumed to be.** The append-vs-rebuild logic in `drawEvents` had already been fixed before this branch. What remained — measured at 8,042 DOM mutations per ten idle seconds, against 6 for Dashboard and 10 for Settings — was `drawEvents` calling `App.wireRowKeyboard` on every 100 ms tick regardless of whether anything had changed, and that function walks the whole table stamping `tabindex` on every row, up to the 2,000-row cap. `b5d6e654` made the call conditional on rows actually having been added or the selection having moved (871→367 mutations per 10 s on a paused tab); `1534a60f` closed the remainder by making `wireRowKeyboard` itself skip a `tabindex` write when the value is already correct, which is the change that mattered most — measured on a live event table, 5,472 writes per 30 seconds became 0, with events still arriving.

---

## Defects found and fixed, by phase

### Phase 1 — traps, silent writes, five asks (complete)

All eleven findings landed, principally in `4c28d84c` and `291fd453`, with a reconciliation pass in `3dd0aaf7` after three workers kept refining the same files past their first commit.

- **P1-1, the Account trap.** `accountBtn.onclick = accountModal` handed the click's `MouseEvent` to `accountModal(forced)`, so every Account press opened the forced, unclosable first-sign-in dialog. `4c28d84` makes the handler call `accountModal()` with no argument, turns the forced path into an explicit option, and moves `setBackgroundInert(false)` in `closeModal` ahead of the locked-dialog early return, so a locked dialog can no longer strand the page.
- **P1-2, discovery already-added.** A sweep of an already-monitored range ticked 56 checkboxes for devices already added. `4c28d84` computes `existing_device_id`/`existing_device_name` once per listing; already-monitored rows render "Already added — `<name>`" with a link and no checkbox, are excluded from select-all, and "Add approved" reports how many were genuinely new. Covered by `tests/test_nodediscover_e2e.py`.
- **P1-3, tab renames.** WIRELESS (FORTIGATE) → FORTIWIRELESS, CONFIGRX → ConfigRX, NETPATH → ROUTES, landed in `4c28d84`; the README/FEATURES tab lists followed in `52c243a4`, with a clause in each noting Routes is the NetPath module under a clearer label.
- **P1-4, the WEB button.** Device details gained a WEB link to `http://<ip>/` (IPv6 bracketed) beside SSH, in `4c28d84`; `3dd0aaf7` found that `.linkish` only matched `button.linkish` and the new `<a>` rendered as browser-default blue, and extended the selector to cover both.
- **P1-5, dialog buttons below the fold.** `4c28d84` inverts `App.modal`'s default so the button row sits at the top unless a caller opts out.
- **P1-6, validation and error routing.** The modal error paragraph gained `id="modal-error"` with `aria-describedby` wired to invalid fields, and the sign-in page did the same, in `4c28d84`. `291fd453` added the server- and client-side pieces: ConfigRX's SSH port refused outside 1–65535 on both sides, NetPath's Add validates the host both sides instead of storing `999.999.999.999`, and Add device routes a blank address or a server refusal through `requireFields` and the modal error line.
- **P1-7, Settings.** `291fd453` added a client-side range check before POST, a server-side range guard, and a `saved` snapshot so Revert repaints from the last saved payload instead of the mutated live state; `3dd0aaf7` names the mechanism `APPLY_FIELDS` and adds the dirty-tab guard that marks unsaved edits, keeps them across a tab switch, and announces them on leaving.
- **P1-8, ConfigRX viewer 403.** `291fd453` moves the stored-backup route to ConfigRX read, redacting a verbatim-stored row for callers without write, so a reader's click toasts instead of throwing an uncaught 403. `3dd0aaf7` records that this specific change had to be recovered from the pushed commit rather than the working tree, which had lost it.
- **P1-9, the discovery ambush.** `4c28d84` replaces the every-visit 62-row approval dialog with a dismissible strip line, auto-opened only for the session that started the scan and only once.
- **P1-10, IPAM's default tab.** `291fd453` reorders IPAM to open on Subnets & hosts, with DHCP disabled and its reason stated where DHCP is unavailable; `4c28d84` sets the product-wide default landing tab to Dashboard.
- **P1-11, Cancel/Close/primary hygiene.** `291fd453` gives Maintenance windows and the Wireless controllers dialog a real primary and a Cancel; `4c28d84` wires kiosk mode so a non-`kioskSafe` dialog degrades to a toast rather than blocking a wall display, with only the forced password dialog marked safe.

### Phase 2 — ConfigRX (complete)

The backend half — six new Cisco platform personas (NX-OS, IOS-XR, a small-business switch, an ASA, a WLC), enable-mode escalation, and the encrypted enable secret's storage contract — landed in `46e47ec9`, with `tests/test_configrx_cisco_platforms.py` driving every persona through the real capture chain plus one full `ConfigRxWorker.backup_now` run.

The UI half, listed as TODO in the recovered findings, landed in `b245e526`:

- The credential route accepts `enable_secret` end to end — absent leaves a stored one alone, empty clears it, non-empty is encrypted like the password — and the credential dialog surfaces it with a hint that it is only needed where a platform lands in user EXEC. The vendor field became a list of the eleven known keys with a free-text escape.
- "Back up now" is disabled with its reason instead of hidden outright, without fighting the permission gate's own disabling; a failure toasts the device and the reason, rather than only flipping the button label to "Failed".
- A capture under a fifth of the previous stored size is stored but flagged `suspect` rather than `changed`, so the next diff does not present an entire configuration as deleted.
- Port and Credential — the two facts that decide whether a backup can work at all — are visible columns by default; the list defaults to the devices ConfigRX actually manages; the backups header wraps instead of clipping its last button.

The same commit fixed the intermittent failure in the new Cisco test suite — the `_pull_config` crash described above — and a second race in the suite itself (a worker's first due-scan landing between enabling backups and storing the credential); 35 consecutive standalone runs and two full suite runs were clean afterward.

### Phase 3 — keyboard and screen-reader semantics (mostly complete)

Landed across three commits: `e468ca8e` (the tab/subtab contract, focus handling, SVG pattern collisions, the idle banner, the denied-control note), `b9785c4c` (every chart, the route graph, the topology view and the SSH terminal reachable without a pointer), and `5c88ab88` (the terminal's keyboard-trap exit).

- The tab strip has carried `role="tablist"` for releases without implementing Arrow/Home/End or roving tabindex; `e468ca8` adds both, and extends the same shared handler to all eleven `.subtabs` groups, which had no role, no selected state and no arrow keys at all.
- Eight charts, the route graph, the timeline and the topology view were `tabIndex -1` with tooltips bound to `mousemove` alone. `b9785c4` gives every chart container `tabindex`, `role="img"` and a label built from the module's own header summary; tooltips answer focus as well as hover in netflow, ipam, netpath, nodes, snmp and syslog; keyboard movement was fitted to each chart's own idiom (NetFlow's existing pan/zoom handlers take arrow keys, NetPath's timeline walks bucket by bucket, IPAM's scope trend walks point by point). NetPath's destination list — the control that decides what the whole module is about — became a real listbox with roving tabindex keyed by destination id.
- The SSH terminal gained `role="status"`, a visually hidden `role="log"`, xterm's `screenReaderMode`, one correct tab stop instead of two, disposal of the previous terminal before a reconnect, and a socket-failure message that names the host, port and where to change it instead of `[Errno None] Unable to connect to port 2201 on 127.0.0.250` (`b9785c4`). Escape had been swallowed as the terminal's only documented way out, which is a trap: Escape is a real keystroke inside vi, less and the menu-driven consoles this window exists to reach. `5c88ab8` makes Ctrl+F6 the exit and frees Escape to reach the device.
- Six SVG pattern ids were emitted identically on every page, so `url(#sw-pat-hatch)` resolved to whichever `<defs>` came first — the one mechanism carrying status without relying on hue rested on an id collision. Fixed by suffixing ids per host SVG (`e468ca8`, completed in `b9785c4` for the two remaining `statusPatternUrl` call sites).
- Also in `e468ca8`: the discard prompt now marks the form behind it `inert` rather than leaving thirteen fields focusable; sorting a column restores focus to the header afterward instead of destroying it; Space on a focused row ticks its checkbox instead of opening it; the idle banner's `role="alert"` announces checkpoints rather than every second; the denied-control sentence is one note per denied module per page instead of one per container with no gutter; and the Alerts tab's accessible name, which read "ALERTS76", was fixed.

Two of the three items this phase's list left open have since closed. The device-detail dialog's missing `options.trigger` was fixed in `ff44261a`, once its actual cause was found: the dialog opens on the *first* click of the double click that also selects the row and redraws the table, so by the time the handler ran, whatever had focus no longer existed — the row is now passed explicitly, and survives the redraw because rows are patched in place rather than rebuilt (a change Phase 7 made for other reasons). The three placeholder-only inputs did close, but not with any note taken of it: `b245e526` — a commit whose own message is entirely about the ConfigRX channel-close defect — also gave `new-username`/`new-password`, `ldap-test-username`/`ldap-test-password` and the three token-creation fields `aria-label`s, confirmed present in `index.html` today. It is recorded here as done and unclaimed rather than as done and described, which is what checking the code rather than the message is for. Keyboard number-shortcuts remain open: `event.key < '1' || event.key > '9'` in `app.js` is unchanged, so the mechanism still cannot reach three of the twelve tabs.

### Phase 4 — feedback, outcomes, form consistency (primitives and module adoption complete; a substantial tail of copy and interaction findings remains open)

`1534a60f` built the shared pieces this phase needs, generalising the one good async-job pattern already in the product — ConfigRX's "Back up now" state machine — into `App.runJob(button, labels, promise)`; adding `App.emptyRow`, wired through `drawRows`, so a table cannot ship a header row over an empty body; a `button.danger` tier, pushed away from a dialog's affirmative end (`.modal-box .row button.danger { margin-right: auto }`) rather than sitting flush against Save; a two-track label-column grid so a control's left edge stops spreading 42px within one fieldset and 82px across Settings; a 68-character prose cap in place of the old 250-character measure; `.linkish.inline` for a mid-sentence link that should not carry button padding; `.scrollbox` in place of five ad-hoc heights; and an Account dialog that names who is signed in, what access they hold and when the session ends, rather than being titled "Change your password" and saying nothing about the account.

`b530718` is the module adoption that the first pass of this document could not yet see, because it had not been committed. Verified against the source rather than the message: Add device, Acknowledge, bulk Acknowledge, Trace now and every module's Settings Save now run through `App.runJob`, confirmed by its call sites in `alerts.js` — the sharpest case was the detail pane going on offering "Acknowledge" after the acknowledgement had already succeeded; Syslog's table no longer ships a header row over an empty body, confirmed by the new `noMessagesText()` naming the searched window instead of saying only "no messages"; the interfaces table names the SNMP failure that produced an empty one, confirmed by the `snmp_error` parameter `b530718` threads through `drawIfaceTable` and the device dialog alike; and five detail panes that rendered a bare key now link it, confirmed directly in the diff — an alert's object to its device, a Syslog or trap source to the device it matches (or an offer to add it, when it matches nothing), a wireless AP to its controller, an IPAM conflict's MACs into MAC search.

Two findings this document's first pass would have been tempted to credit to the same commit do not belong to it, checked by blame rather than assumed from a plausible-looking match. `syslog.js` and `snmp.js` already called `App.countLabel` for their "N of M shown" figure, and `alerts.js` already rendered its pager as "1–300 of 4,120", both since before this branch (`c550395`, `ee1394c`); nothing here touches either call site, so wherever the "'N shown' without a denominator" finding was actually looking, it stays open below. And Nodes' MAC-search "no hit" note — `resolveMacSearch`'s two-branch message naming either an unlearned address or MAC learning being off — is byte-for-byte the code already shipping at `fc6b3e1` (traced to `a0726d9`, long before this branch); it was never broken during the period this document covers, and crediting it to `b530718` would have been describing an accident of proximity in the same file as a fix. What the same function still does not explain, on this branch or any before it: `if (!payload.mac) return;` leaves the note hidden with nothing said when the searched text does not parse as a MAC at all, which may be what the original finding meant and remains genuinely unaddressed either way. Both corrections are made here rather than left as an earlier draft of this document had them.

The margin-right-auto rule for `button.danger` did land, but two findings this document previously listed as addressed by it were checked directly against the Edit device dialog and are not: `editDevice()`'s button array still puts Cancel, Clear credential, Remove, Test and Save in one row with no `danger: true` on either destructive verb, and Remove is still reachable only from inside Edit rather than from the device's own action row. Also still open, checked the same way: Alerts' Clear omits `alerts-filter-state` from its `clears` list while resetting every other filter — the array in `alerts.js` names four fields, not five; a bulk-mute refusal still raises the literal `device_ids and/or group_id is required` in `api.py` (client-side validation catches the normal case before it reaches the server, which predates this branch and was not part of it); template Preview still saves the template as a side effect (`saveTemplate(box, t, true)`, unchanged); Dashboard severity tiles are untouched — `dashboard.js` has no commit against it anywhere on this branch — and still land on a list filtered by whatever State was previously set; NetFlow's status line still falls back to "no template yet" for as long as no template has arrived, which is accurate but was flagged for how little it tells an operator, and nothing changed that judgement. Nodes' identity line still reads "error authorization error": `deviceSummaryHtml`'s `field('error', d.snmp_error, 'nd-err')` (`nodes.js`) is byte-for-byte unchanged from the 4.47.0 baseline, labelling the field "error" beside a backend message that already starts with the word. The 61 inline `style=` attributes are fewer since `.scrollbox` absorbed several of them, but the count has not gone to zero.

### Phase 5 — colour, themes, charts (complete)

- **Light theme contrast.** `36a07af` found the cause was the token test's own pair list, which only ever checked a semantic tone against `--bg`; `--raised` is where those tones actually live (every alternate table row), and `--ok`/`--warn` measured 4.31:1/4.21:1 there — under AA, on a suite reporting green. `TEXT_ON` now checks every semantic tone against `--raised`, `--panel` and `--selected`; the tones themselves moved to `#146530` and `#7F520C` (7.16:1 and 6.74:1, confirmed in `tokens.css`). The light theme's inverted elevation (`--panel` at 92.0% luminance and `--raised` at 84.3%, both darker than a pure-white `--bg`) was corrected by tinting the page (`--bg: #EEF1F5`) and letting surfaces climb to white.
- **High contrast's own weak spot.** The theme whose purpose is separation had the weakest surface separation of the three — `--bg`→`--raised` measured 1.09 against 1.18 in the other two themes. Now 1.21, per the comment in `tokens.css` beside `:root[data-theme="contrast"]`.
- **The `--checked-strong` exception**, recorded rather than quietly held (see below).
- **The route canvas.** `317ed92` gives high contrast its own `--canvas-*` palette rather than leaving it the shared light one — a 1500×540 white rectangle was the single largest glare source in a near-black interface, on the one theme an operator picks because brightness and separation are the problem. The dark theme was deliberately left alone (the canvas is a light palette on purpose — it is what prints), and the light theme needed no change once its own surfaces climbed to white, which gave the canvas a boundary against the page it sits in.
- **The meter, `2857c90`.** An independent pass re-measured the fill-versus-track pair the first fix had left standing at 1.63–2.82:1, under the 3:1 floor for a mark that carries meaning — what had actually been fixed was the track against its *surface*, a different pair. No single track colour clears 3:1 against all four semantic fills and the panel behind it at once, so the track instead takes the raised surface and grows a `--line` border, giving the fill the whole contrast range to itself: confirmed in `app.css`, 6.3:1 measured live.
- **Charts and status, `1c309c9`.** IPAM was drawing its up/down vocabulary inverted — a live host in `--blocked`, the alarm orange, a dead one in the quietest tone on the page — because it was the one data module not using `App.statusMark`; both its tables now call it, confirmed in `ipam.js` (`alive ? 'ok' : (last_up ? 'fail' : 'none')`, matching the legend rather than borrowing syslog severity). The device status timeline gained a time axis and a flat, textureless `--nodata` fill so absence and "sampled, unknown" stop being the same picture. Histograms now plot the populated range rather than the filter window when the two diverge sharply. NetPath's RTT lane no longer reports "peak 0 ms" for loopback times that round to zero, and `fit()` no longer clamps at 100%, confirmed against `netpath.js`. IPAM's donuts separate their arcs and centre a figure. A zero-link topology draws a stated empty state instead of a lattice of empty boxes. The SSH terminal's sixteen ANSI slots resolve from theme tokens instead of `brightWhite` being a hard `#FFFFFF` that measured 1.00:1 on the light theme.
- **The NetPath timeline claim, corrected rather than repeated.** An earlier pass reported the timeline rebuilding a hundred times a second; `1c309c9` found it had no `fastTick` registered at all, so that could not have been happening, and gave it a correct one — confirmed in `netpath.js`, `App.pages.netpath.fastTick: drawTimeline`. What the review saw is not reconstructable from here, but what shipped afterward is real and is what is recorded.

Every item this phase's list carried is accounted for; none is left in "What remains open" below.

### Phase 6 — layout, navigation, information architecture (complete, except two items no commit touched and one that closed only halfway)

This phase's whole list was still open when the first pass of this document was written — the heading below is what changed, not the list itself.

- **The tab bar.** `93dd27e` found the bar needed 1,747px and shed the wrong things first: at 1600px it dropped `#conn` — "the one honest signal during an outage", per its own markup comment — and Sign out; at 1366px, the commonest NOC laptop width, it also took Settings and most of Debug, so an operator on the Settings tab could not see that Settings was the active tab. The twelve tabs now sit in four labelled groups (Now, Inventory, Telemetry, Admin) and the utility group — Search, Account, Sign out, the connection indicator — is pinned outside the scrolling region rather than inside it, confirmed by `.tabs-utility` existing in `app.css` alongside `#tabs`. Grouping is presentational: still one `role="tablist"`, twelve `role="tab"`.
- **The Nodes column-width contract and dashboard tile spacing.** The same commit gives the Nodes table's panes `data-grow` values (confirmed in `index.html`) so its columns get the room they ask for rather than clipping the last one, and the dashboard grid stops leaving 120px holes.
- **Subtab URL state and the Settings reorganisation.** Switching to a subtab used to leave the URL describing a screen that was not on screen; the routing is now generic, so `#/settings/users` is addressable, and Settings — eleven fieldsets over 3,760px with no way to navigate it, opened on a per-browser cosmetic control sitting above every server setting — became the same subtab bar every other multi-section tab has, with Appearance moved into the Account dialog and a Modules pane listing all nine module-settings dialogs in one place.
- **Permission presets.** The permission grid gained role presets, a copy-from picker and a generated initial password in place of twelve modules by three levels of raw radios (`93dd27e`).
- **Cross-module links on the five detail panes**, landed alongside Phase 4's module adoption in `b530718` and described there: an alert's object to its device, a Syslog or trap source to the device (or an offer to add it), a wireless AP to its controller, an IPAM conflict's MACs into MAC search.
- **Global search and kiosk entry/rotation from the screen.** `93dd27e` adds a search on `/` across devices, MACs, interfaces, alerts and destinations, routed through the hash URLs, and kiosk mode enterable from the Account dialog with `?rotate=a,b&every=N`, dot navigation and a countdown. An independent pass afterward (`2857c90`) found the search dialog declared itself modal without behaving as one — `aria-modal` set, the background never marked inert, opened by a bare `/` with no trigger to return focus to on Escape — and fixed the omission; a further pass (`c05ed12`, see below) restored a focus-visible outline on the search input that an earlier `outline: none` had removed along with the rest of the dashboard's box-shadow rings, confirmed in `app.css`.
- **NetFlow's "Resolve names".** No longer a filter-bar checkbox writing server-wide state from what read as a personal view filter; Settings already had the correctly-scoped control, and the checkbox now points there (`b530718`).
- **The 320px breakpoint.** `2857c90` found three screens actually scrolled sideways at it — Settings measured 637px, Nodes 371, NetFlow 478, from subtab toolbars that never got the overflow treatment the top-level tab strip had — plus a fourth cause worth naming on its own: Alerts' visually hidden histogram table, clipped to a pixel by absolute positioning, laid out at full width against the page itself because a table in auto layout ignores a width narrower than its content. All twelve tabs now measure exactly 320px.

Two items remain open because nothing in this branch's work was aimed at them: **help-topic coverage beyond Nodes** — `App.helpLink` is still called only from `nodes.js`, confirmed by a repo-wide search — and **discovery timing fields in a self-opening second dialog, and raw enums in Add rule**, for which no commit touches the relevant code. **Login-page copy** is half-done: `717098b`/`93dd27e` add the version line, wired from `/api/session`'s now-unauthenticated-safe response and verified live; the first-run guidance FINDINGS asked for turns out to already have existed in `login.html` before this branch (`#login-note`, "First time here? Sign in as admin..."), conditionally hidden, and this branch neither touched nor investigated why it stays hidden — that half of the finding is not this release's to claim.

### Phase 7 — performance (complete, except for one item this branch could not close and one figure it should not have claimed to)

- **Debug's churn**, described above, is fixed (`b5d6e654`, `1534a60f`).
- **Static asset caching, switched on.** `b5d6e654` added the `versioned` path to `Handler._static`; the first pass of this document found nothing in `index.html` requesting it yet. `36334ea` closes that gap: `__version__` is `4.48.0`, the markup asks for every asset as `?v=__SW_VERSION__`, and the placeholder is substituted with the running version as each HTML file is read into the static cache, once per load, before the ETag and gzip are computed. Verified in a browser rather than by reading, per the commit: seventeen asset requests, all seventeen carrying `?v=4.48.0`, all seventeen answered `immutable`, no placeholder surviving into the markup a browser receives.
- **`fastTick` write-on-change.** `1c309c9` gave the NetPath timeline a correct `fastTick` (see the Phase 5 correction above) and extended the write-on-change discipline elsewhere; idle DOM mutations per ten seconds fell across every module measured (nodes 515→118, ipam 500→0, alerts 767→60, snmp/syslog/netflow/wireless 500ish→10), per `47996db`.
- **The spacing scale, `--fs-lg`, and `<code>`.** `910aad4` replaced twenty distinct padding/margin values and eight gaps with a four-step scale (`--space-xs/sm/md/lg`, confirmed in `tokens.css`), gave `<code>` its own styling, and put `--fs-lg` — documented as the dialog-heading size and used by no dialog heading — onto an actual heading. The same commit added colour transitions, deliberately held back until the DOM-churn work above stopped cancelling them mid-flight, scoped to `prefers-reduced-motion: no-preference`.
- **The Nodes-refresh figure — a correction, not a fix.** `910aad4`'s own message credits its `drawRows` change with fixing "a Nodes refresh cost 154ms of main thread for 61 rows... on the refresh timer". Checked against the code rather than taken from the message: the Nodes device table does not call `App.drawRows` at all — it has built and cached its own `<tr>`s in `nodes.js` since before this branch existed (`rowCache`, `drawTable()`, present already at `fc6b3e1`). `c05ed12` says as much directly, in a comment added to `app.js`: "The 154ms-per-refresh figure quoted for this was measured on the Nodes DEVICE table, which does not come through here... Stated because a comment claiming someone else's number is the kind of thing this file exists not to do." What `drawRows`' own row-diffing genuinely sped up is the interface lists, discovery results, syslog, traps and flow records — real tables, real savings, just not the one the number was measured on. Whether Nodes' pre-existing per-row cache actually holds the device table's own refresh to something better than 154ms was not re-measured by any commit on this branch, and is not claimed here either way. The item stays in "What remains open" below, now with this history attached rather than the earlier attribution.

**PERF-004, minification, was declined.** The application is stdlib-only Python with vanilla JS/CSS and no build step; gzip is already negotiated in `_send` at the server's own `GZIP_LEVEL = 6` (`netpath/web/server.py`). Measured directly against the 4.47.0 build this review was run on — the fourteen files a cold load actually pulls in (`index.html`, `boot.js`, `app.js`, the twelve module scripts, `tokens.css`, `app.css`), gzipped at that level — the cold load is about 285 KB. A build step buys marginal bytes on top of a compression scheme that is already doing the real work, for a project that otherwise ships with nothing to compile; the cache work above was judged the higher-value use of the same budget.

That figure is the one the decision was argued from, and it has since moved: the same fourteen files at the end of this release gzip to roughly 324 KB, because the accessibility and feature work described above added to every one of them. The reasoning is unchanged — there is still nothing to compile and compression is still doing the real work — but a reader comparing this release against a future one should start from 324 KB, not 285. If the front end keeps growing at this rate the decision is worth taking again rather than inheriting.

### Test infrastructure and Windows portability

Four commits outside the phase numbering fixed the harness itself rather than the product, all found chasing the Windows leg of the test matrix:

- `d026361` — `tests/run_all.py` decoded subprocess output using the locale encoding (cp1252 on Windows), so the first suite to print an em dash raised `UnicodeDecodeError` inside subprocess's reader thread and took the runner down with a `TypeError` before printing a single PASS or FAIL. Now decodes UTF-8 with `errors="replace"`.
- `695573c` — three suites that only passed on the machine they were written on: `test_icmp_socket.py` asserted a refused ICMP socket returns `False`, which only holds where `ping` is absent (not true on Windows or most Linux hosts) and fed Unix `ping` output to the Windows RTT parser; `test_secretstore.py` called real Windows DPAPI where it meant to test the portable `NPSS` path; `test_static_headers.py` guarded the wrong endpoint against a payload-size ceiling.
- `17aee34` — the WebSocket close-frame defect described above, found while chasing the last intermittent failure in this list, which took the Windows run to 53 of 53.
- `c376289` — the runner fix above covered a child's output being decoded on the way out; it did not cover a child crashing on its own `print()`. `test_db_reclaim.py` writes a `✓` per assertion, and a cp1252 console — which is what Windows stdout is by default, piped or not — raises `UnicodeEncodeError` on it before a single result prints, whether or not `run_all.py` is the one running it. The suite now reconfigures its own stdout to replace what it cannot encode, which works identically run standalone. A full scan while fixing this corrected the earlier commits' own account of the cause: em dashes were never the problem, since Windows-1252 represents them fine; the actual gap is the narrower set of characters (smart quotes aside) cp1252 has no slot for at all, of which this tick mark was the only one that could crash anything.

`3dd0aaf7` records the Windows suite state after Phase 1's reconciliation: 51 of 53, with `test_ssh_terminal.py` passing alone and failing only under full-suite load, and the then-new `test_configrx_cisco_platforms.py` intermittent against its in-process paramiko stub — both later resolved (`46e47ec9` — the standalone `test_ssh_terminal.py` behaviour is unchanged and not further addressed here — and `b245e526` for the ConfigRX flake).

Outside the test suites proper, the demo/fleet harness got two fixes of its own worth recording here because they would otherwise cost a reviewer real time chasing a symptom two layers removed from its cause: `af5f868` moved `fake_ssh.py`'s port binding into the parent process, ahead of the "listening" announcement every manual run and `scenario.py` poll for, and added `SO_EXCLUSIVEADDRUSE` on Windows so a colliding second instance fails loudly instead of `SO_REUSEADDR` letting it silently steal another process's connections; `a0ed169` gave the demo a device the fake harness can actually answer (`configrx-ssh-01`) and pointed it at the ASA persona so the enable-mode escalation ConfigRX gained this release is walked for real during seeding, not asserted only by unit tests.

### Phase 8 — release mechanics (mostly complete; the merge is the one step left)

The version bump landed in `36334ea` (`netpath/__init__.py` reads `4.48.0`, confirmed) and is now load-bearing rather than bookkeeping, since it drives every asset's cache-busting query string. The CHANGELOG entry is `37ba9fb`, confirmed present at the top of `CHANGELOG.md`'s Contents. `tests/README.md` carries the row for `test_configrx_cisco_platforms.py` (`46e47ec9`) and gained the row `test_web_gates.py` never had, plus updated entries for the security- and WebSocket-related suites (`c376289`).

A read-only security review of the whole change set, run before any of it reaches `main`, is `3c8daedc`. It confirmed the parts that matter most were sound — no path returns backup content unredacted to a caller without ConfigRX write, an API token inherits its account's permissions rather than escaping them, the free-text vendor field cannot reach a device's shell, the versioned-immutable asset path cannot poison a cache, the WEB link leaks nothing through `Referer` or `window.opener` — and found three things fixed in the same commit: clearing a device's credential left the encrypted enable secret behind (`clear_credential` nulled only `ssh_password_enc`; both go together now, confirmed in `configrxdb.py`/`api.py`); `_drain()` could borrow another thread's `SEND_TIMEOUT_S` rather than using its own, since a socket's timeout belongs to the socket and not the caller; and the drain had no test of its own, being new and security-adjacent — three landed in `test_wsock.py`. It also raised a policy question rather than a defect — a redacted ConfigRX capture still carries interface addressing, ACLs, routes, VPN peers and management addresses, so moving backup content from ConfigRX write to ConfigRX read widened what a read-only account can see — which `fe091d80` answered with a named decision recorded beside the route (keep it: seeing what changed without holding the permission to change it is what the module is for) rather than leaving it implicit, and `13f274a` brought `CREDENTIAL-SECURITY.md` and `INTERNALS.md` back into line with what the branch had changed underneath them by that point.

This document is Phase 8's "consolidated review doc" item. The second, later pass described next found five further defects — in how the fix work was done, not in what it was asked to do — and called the branch unfit to merge as it stood. `c05ed12` fixes all five.

A third and last pass, after this document had already been brought level with the branch at `0a907b3`, read the seven module files against each other rather than each against the brief, and found the cost of having handed one file to one worker: the same logic written out three, five and seven times. A device-by-address lookup existed five times, each with its own fetch of the whole device list on its own clock and its own answer to the same question; the histogram range narrower three times, each copy carrying a comment admitting it had been written independently "rather than a shared one in app.js"; the write-only-if-changed guard seven times. `6fab3b5` consolidates all three into `app.js` and adds a contract check for each, written against the shape of the duplication rather than its name, since a rule that only catches `loadDeviceByIp` is defeated by the next person writing `deviceForIp`. Two further copies were found in the same read and closed by `94db199` — `configrx.js` kept a hand-maintained duplicate of `configrx_vendors.VENDORS`, which is now served by `/api/config` (key and label only), and a stored enable secret could still not be dropped without re-typing the SSH password nobody has, which a narrow `DELETE .../credential/enable-secret` now allows. `8662e2d` removed a NetFlow filter checkbox no code had read since resolution became a Settings option.

`588eac0` is the last commit before the merge, and is harness rather than product: `demo/fake_ssh.py` generated a fresh host key at every start, so the second run of the demo environment always ended with ConfigRX refusing the backup as a changed host key — the check working exactly as designed, against a double that was misbehaving. The personas keep one key per machine now.

The merge to `main` follows this revision, gated on the full suite and the browser walk: 53/53 backend suites and 52/52 browser checks, with zero console errors, zero page errors and zero responses at or above 400 on both the admin and the read-only account.

---

## A second pass, after the branch looked finished

The security review above was aimed at whether the change set was safe to merge. A further, broader pass — across the branch as a whole, once the fix work substantially looked done — found five defects in how that work was carried out, and, in its own words, called the branch unfit to merge as it stood. All five are fixed in `c05ed12`, whose own message reports the gates run afterward: 53 of 53 test suites, 52 of 52 browser-walk checks, no console error or 4xx as a read-only account.

- **A shared row cache let the device dialog steal the interface pane's rows.** `910aad4`'s `drawRows` optimisation (Phase 7) keyed its reused `<tr>`s by column set alone, on the reasoning — recorded in its own comment — that no two tables in the app share a column set. They do: the interface list is drawn both in the Nodes detail pane and inside the device dialog, from the same column descriptors. Appending a cached DOM node *moves* it rather than copying it, so the two tables were taking rows off each other — opening the dialog emptied the pane, and the next poll emptied the dialog. `c05ed12`'s own account of reproducing it before the fix: pane 50 rows, open the dialog and the pane drops to 0 with no empty state, refresh and the dialog drops to 0 instead. The cache is now a `WeakMap` keyed by the table element first and the column set second, confirmed in `app.js`, with a new browser-walk check (`tests/ui/walk.mjs`, "the device dialog does not take the pane's interface rows") that opens a device, counts the pane's interface rows, opens the dialog, and asserts both counts are still what they were.
- **Settings' dirty-tracking covered only its first subtab.** The guard that marks unsaved Apply-field edits and warns before they are discarded was wired to `#page-settings .scroll` — the first scroll container on the page. Once Settings became seven subtabs (Phase 6), each with its own scroll container, that listener only ever saw General; an edit to a Retention cap or the Sign-in idle timeout could be navigated away from with no warning at all, which is the exact failure the guard exists to prevent. It now delegates from `#page-settings` itself, confirmed in `settings.js`.
- **Denied-permission notes could land on a hidden subpage.** The one-note-per-denied-module mechanism (Phase 3) grouped notes by page; once Settings had subtabs, a note explaining why a Users-tab control was disabled could be written into General's hidden markup, where nothing reading the page on screen would ever see it. Notes are now grouped by the nearest `.subpage` where one exists, confirmed in `app.js`. The same commit found `setBackgroundInert` was leaving `.tabs-utility` — Search, Account, Sign out, the connection indicator, all pinned outside the scrolling tab strip since Phase 6's regrouping — reachable by a screen reader behind an open dialog's scrim, and added it alongside `#tabs` to the inert set.
- **The WebSocket drain could run on the wrong thread.** This is the third defect found in the same mechanism over the course of this branch: `17aee34` added the drain itself, `3c8daedc` fixed it borrowing another thread's send timeout, and this pass found `close()` can be called from a thread other than the one parked in `recv()` — the idle watchdog, the registry's shutdown, the SSH pump reporting a session ended, all call `close()` from outside the reader — and reading the same socket from two threads at once is not merely wasted work under TLS, where two threads inside one SSL object corrupt state rather than raising something catchable. The reader thread now records itself on the socket; `close()` drains immediately only when it is that thread or no read has started yet, and otherwise sets a flag the reader consumes on its own way out of `recv()`, confirmed in `wsock.py`. A new `test_wsock.py` case parks a reader in `recv()`, closes from a second thread, and asserts the drain happened anyway and both threads exit — and, per the commit's own account, caught its own first draft, which sent bytes that ended the reader before `close()` ever ran.
- **Four documents had drifted again.** `CREDENTIAL-SECURITY.md`'s account of `DELETE .../credential` still matched the pre-security-review behaviour (clearing only the SSH password); `INTERNALS.md` still described the versioned-asset markup as not yet wired to anything, after `36334ea` wired it; `PERFORMANCE_REVIEW.md` still said the same wiring was "tracked as remaining Phase 8 work" after it had landed. All three are corrected in `c05ed12`, each against the commit that actually changed the behaviour rather than against another document's account of it. This document is the fourth: the mid-branch snapshot this revision replaces is the drift the same pass was describing when it named this file specifically.

Also in `c05ed12`, named in its own message rather than left for this document to notice on its own: the edit route accepted a non-string `host` where the add route stringifies first (`api.py`); the `button.danger { margin-right: auto }` rule from Phase 4 (pushing a destructive button away from a dialog's affirmative end) was pulling a two-button Cancel/destructive confirm apart into the middle of a wide dialog instead of grouping the pair, since there was nothing past the danger button to be pushed away from — it now applies only when the danger button is not the last one (`app.css`); and `netpath/configrxdb.py`'s schema comment for `enable_secret_enc` was reworded for precision — what happens when a vendor needing enable mode has no secret stored is an escalation attempted with an empty password, not a guess avoided — without changing behaviour.

---

## The `--checked-strong` exception

`36a07af` found, and `tokens.css` now records in a comment beside the token block, that `--checked-strong` — the tint for a row that is both ticked for bulk action and open in the detail pane — cannot be made to clear AA on every surface without giving something else up. It is deliberately the strongest of the three selection tints, because "ticked and open" has to be unmistakable, and contrast is a luminance relationship: solving for a value where every tone clears 4.5:1 against it lands all three tints within 0.03 of each other in the dark theme, or inverts their order in the light theme. Either result defeats the reason the tint exists. The decision recorded in `tokens.css`: `--text` (7.45:1 dark, 11.27:1 light) and `--muted` (light) hold at AA on `--checked-strong`; the quietest metadata tone, `--dim`, does not, so nothing that only appears in `--dim` is drawn on a row in that state; and the high-contrast theme, which claims AAA everywhere else, was moved (`--checked-strong: #2A3E66`, at 1.55 against `--raised` where `--checked` alone is 1.27) until it clears its own bar outright.

---

## What remains open

Compiled from FINDINGS.md against what the commit history above shows landed, checked against the code in every row below rather than against a commit's account of itself. Phases 1, 2 and 5 no longer appear here at all: every item FINDINGS.md carried for them is closed, and their phase sections above say what closed them. The five defects the second review pass found (row cache, Settings dirty-tracking, denied-permission routing, the WebSocket drain's thread ownership, four documents' drift) are not FINDINGS.md items and are not repeated here — they have their own section above, and are closed, fixed in `c05ed12`.

| Phase | Item | Status |
|---|---|---|
| 3 | Keyboard number-shortcuts cover 9 of 12 tabs (`event.key < '1' \|\| event.key > '9'`, `app.js`, unchanged) | Open |
| 4 | Alerts Clear omits `alerts-filter-state` from its `clears` list | Open |
| 4 | "N shown" without a denominator (wherever the original finding was looking — not `syslog.js`/`snmp.js`'s `App.countLabel` calls or `alerts.js`'s pager, which predate this branch and are unchanged) | Open, location unconfirmed |
| 4 | Dashboard severity tiles land on a list still filtered by the previous State (`dashboard.js` untouched all branch) | Open |
| 4 | Bulk mute error leaks `device_ids and/or group_id is required` if the client-side guard is bypassed | Open |
| 4 | Destructive verbs (Remove, Clear credential) share a button row with Save in Edit device, with no `danger: true` on either | Open |
| 4 | Remove device reachable only from inside the Edit dialog, not from the device's own action row | Open |
| 4 | Template Preview still saves the template as a side effect (`saveTemplate(box, t, true)`) | Open |
| 4 | NetFlow status line still reads "no template yet" for as long as none has arrived; Nodes identity line still reads "error authorization error" (`field('error', d.snmp_error)`, unchanged) | Open |
| 4 | Inline `style=` attributes, down from 61 but not eliminated | Open (reduced; partially superseded by `.scrollbox`) |
| 6 | Help coverage: `App.helpLink` called only from `nodes.js` | Open |
| 6 | Discovery timing fields in a self-opening second dialog; raw enums in Add rule | Open |
| 6 | Login page first-run guidance (`#login-note`, present since before this branch, conditionally hidden; not investigated by it) | Open |
| 7 | Nodes refresh cost under full teardown-rebuild — see the Phase 7 correction above; not measured again since, by any commit, committed or not | Open |
| 8 | Merge to `main` | Closed — merged at the branch tip after both gates ran clean (53/53 suites, 52/52 browser checks) |

Closed since the first pass of this document, with what closed each recorded in its phase section above rather than repeated here: both Phase 3 items previously listed (`ff44261a`, `b245e526`); Phase 4's outcome messages, empty-state tables, five cross-module links and the MAC-search note (`b530718`); all nine remaining Phase 5 items (`2857c90`, `1c309c9`); the whole of Phase 6 except the three rows above (`93dd27e`, `b530718`, `2857c90`); Phase 7's `fastTick` write-on-change, the spacing scale, `--fs-lg`, `<code>` and `prefers-reduced-motion` (`910aad4`, `1c309c9`); and Phase 8's version bump and CHANGELOG entry (`36334ea`, `37ba9fb`). Phase 1's eleven findings and Phase 2's UI TODO list were already closed with nothing remaining against them, and still are.

---

## The review method, and what it missed

The previous review (`UI-UX-REVIEW.md`, build 4.36.1) and this one's screenshot evidence both came from `demo/ui_walk.mjs`, the collector that walks the product and captures a screenshot at each named state so a change can be diffed before and after. `ec3ded8f` found that its inventory had fallen behind what the application had grown to: the Nodes topology subtab, two of the four device-detail nested subtabs, MAC search, device groups, custom MIBs and every ConfigRX dialog were simply absent from it, so a change to any of them was invisible to a before/after comparison. More significantly, it had no theme pass and no viewport pass at all — every capture it had ever taken was the dark theme at one screen size. That is one of the two concrete reasons a light-theme contrast failure (Phase 5, fixed in `36a07af`) and a tab bar that clipped its own utility group below a certain width (Phase 6, fixed in `93dd27e`) both survived a review that used it: the tool literally never looked.

The collector now walks 152 steps: the previously-missing surfaces above, a kiosk pass (which also proves a non-kiosk-safe dialog degrades to a toast rather than blocking a wall display), each of the three themes across all twelve tabs, and three viewports. 151 of 152 ran clean against the live instance on the run recorded in `ec3ded8f`, producing 140 screenshots; the one failure (`dlg:alert-rule`) needs an alert rule to already exist on the seeded instance and is recorded as failed rather than silently skipped, which is the script's own design. Selection throughout is by `data-tab`/`data-subtab` rather than by visible label, specifically so that a renaming pass like this release's tab-name changes cannot silently break the walk's own aim.

What this does not fix: the collector takes screenshots, and a screenshot is still read by a person or by a model looking at it. Extending its inventory closes the class of defect where the tool never pointed a camera at the thing that was wrong; it does not by itself catch a defect the camera saw and nobody read carefully.

A second lesson followed once the fix work itself was reviewed: some of what the second pass found (above) could not have been caught by a better screenshot walk at all, however thorough. A row cache keyed wrong, a dirty-tracking listener bound to the wrong element, a `close()` racing the thread inside `recv()` — none of these change what renders. The device dialog and the interface pane looked correct in any single capture; the failure was in which DOM node ended up in which table, and the WebSocket race depended on which thread reached a line of code first, not on anything a browser paints. `demo/ui_walk.mjs` is a tool for the class of defect a screenshot can show. This release also carried a class it cannot: reading the code that moves state between two things that each look fine on their own, and reading it again after the fix work has had time to touch it a second time. Extending the walk's inventory was the right answer to the first lesson; it is not an answer to the second, and nothing in this document should be read as claiming it is. One item on this list — the Nodes-refresh figure — is a reminder of a third, related failure mode: a commit's own account of what it fixed is itself something to check against the code, not something to carry forward as fact, which is what produced that correction and is why this revision exists at all.
